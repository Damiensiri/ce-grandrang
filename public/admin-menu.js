let password='',pages=[];const login=document.querySelector('#login'),panel=document.querySelector('#menu-panel'),list=document.querySelector('#menu-list'),status=document.querySelector('#status');const auth=()=>({Authorization:`Bearer ${password}`});function draw(){list.innerHTML=pages.map((p,i)=>`<article><strong>${p.menu_label}</strong> <small>/${p.slug}</small><label>Groupe<input data-k="menu_group" data-i="${i}" value="${p.menu_group||''}"></label><label><input data-k="show_in_menu" data-i="${i}" type="checkbox" ${p.show_in_menu?'checked':''}> Visible</label><button data-move="${i},-1" ${i?'':'disabled'}>↑</button><button data-move="${i},1" ${i<pages.length-1?'':'disabled'}>↓</button></article>`).join('');list.querySelectorAll('[data-move]').forEach(b=>b.onclick=()=>{const[i,d]=b.dataset.move.split(',').map(Number);[pages[i],pages[i+d]]=[pages[i+d],pages[i]];draw()});list.querySelectorAll('input[data-k]').forEach(i=>i.onchange=()=>{const p=pages[i.dataset.i];p[i.dataset.k]=i.type==='checkbox'?(i.checked?1:0):i.value})}login.onsubmit=async e=>{e.preventDefault();password=String(new FormData(login).get('password')).trim();const s=await fetch('/api/admin/session',{method:'POST',headers:auth()});if(!s.ok){login.querySelector('output').textContent='Mot de passe incorrect.';return}pages=await fetch('/api/admin/pages',{headers:auth()}).then(r=>r.json());draw();login.hidden=true;panel.hidden=false};document.querySelector('#save-menu').onclick=async()=>{const responses=await Promise.all(pages.map((p,i)=>fetch(`/api/admin/pages/${p.slug}`,{method:'PUT',headers:{...auth(),'Content-Type':'application/json'},body:JSON.stringify({...p,position:(i+1)*10})})));status.textContent=responses.every(r=>r.ok)?'Menu enregistré.':'Une erreur est survenue.'};
