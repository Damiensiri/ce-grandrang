fetch('/api/content').then(r=>r.json()).then(c=>{for(const [key,value] of Object.entries(c)){const id=key.replace('.', '-');const el=document.getElementById(id);if(el) el.textContent=value}if(c['hero.image'])document.querySelector('.hero').style.backgroundImage=`linear-gradient(90deg,rgba(13,28,18,.82),rgba(13,28,18,.25)),url("${c['hero.image']}")`;const button=document.getElementById('hero-button');button.href=c['hero.buttonUrl']||'#';button.textContent=(c['hero.buttonLabel']||'Découvrir le centre')+' ↘'});
