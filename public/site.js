const header=document.querySelector('header');fetch('/api/menu').then(r=>r.json()).then(items=>{const links=items.map(p=>`<a href="/${p.slug}">${p.menu_label}</a>`).join('');header.innerHTML=`<a class="gr-brand" href="/"><img src="/logo-grand-rang.png" alt="Centre Équestre du Grand Rang"></a><nav>${links}</nav><a class="gr-contact" href="/contact">Contact ↗</a><details><summary>Menu</summary>${links}</details>`});fetch('/api/content').then(r=>r.json()).then(c=>{for(const[k,v]of Object.entries(c)){const e=document.getElementById(k.replace('.','-'));if(e)e.textContent=v}const hero=document.querySelector('.hero');if(hero&&c['hero.image'])hero.style.backgroundImage=`linear-gradient(90deg,rgba(3,46,93,.82),rgba(3,46,93,.25)),url("${c['hero.image']}")`;const b=document.getElementById('hero-button');if(b){b.href=c['hero.buttonUrl']||'#';b.textContent=(c['hero.buttonLabel']||'Découvrir')+' ↘'}});
